<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sakila.mapper.InventoryMapper">
	<select id="selectCountInventoryByFilm" parameterType="Integer" resultType="Integer">
		SELECT COUNT(*) 
		FROM inventory
		WHERE film_id = #{filmId}
	</select> 
	
	<select id="select_UnReturned_InventoryListByStore" parameterType="map" resultType="map">
		SELECT
			f.film_id filmId
			, f.title title
			, V6.inventoryId inventoryId
			, V6.storeId storeId
			, V6.inventoryCount inventoryCount
		FROM film f
		INNER join
			(SELECT 
				filmId
				, storeId
				, GROUP_concat(inventoryId) inventoryId
				, COUNT(inventoryId) inventoryCount
			FROM 
				(SELECT 
					i.inventory_id inventoryId
					, i.store_id storeId
					, i.film_id filmId
				FROM inventory i
				INNER JOIN
					(SELECT 
						rental_id
						,inventory_id
						, rental_date
					FROM 
						(SELECT 
								rental_id
								, inventory_id
								, rental_date
								, return_date
						FROM rental V1
						JOIN 
							(SELECT 
								inventory_id inventoryId 
								, MAX(rental_date) maxRentalData
							 FROM rental 
							 GROUP BY inventory_id) V2
						 ON V1.inventory_id = V2.inventoryId AND V1.rental_date = V2.maxRentalData) V3
					 WHERE return_date IS NULL) V4
				 ON i.inventory_id = V4.inventory_id) V5
			GROUP BY storeId, filmId) V6
		ON V6.filmId = f.film_id
		WHERE storeId = #{storeId}
		LIMIT #{beginRow}, #{rowPerPage}
	</select>
	
	<select id="select_Returned_InventoryListByStore" parameterType="map" resultType="map">
		SELECT
			f.film_id filmId
			, f.title title
			, V6.inventoryId inventoryId
			, V6.storeId storeId
			, V6.inventoryCount inventoryCount
		FROM film f
		INNER join
			(SELECT 
				filmId
				, storeId
				, GROUP_concat(inventoryId) inventoryId
				, COUNT(inventoryId) inventoryCount
			FROM 
				(SELECT 
					i.inventory_id inventoryId
					, i.store_id storeId
					, i.film_id filmId
				FROM inventory i
				INNER JOIN
					(SELECT 
						rental_id
						,inventory_id
						, rental_date
					FROM 
						(SELECT 
								rental_id
								, inventory_id
								, rental_date
								, return_date
						FROM rental V1
						JOIN 
							(SELECT 
								inventory_id inventoryId 
								, MAX(rental_date) maxRentalData
							 FROM rental 
							 GROUP BY inventory_id) V2
						 ON V1.inventory_id = V2.inventoryId AND V1.rental_date = V2.maxRentalData) V3
					 WHERE return_date IS NOT NULL) V4
				 ON i.inventory_id = V4.inventory_id) V5
			GROUP BY storeId, filmId) V6
		ON V6.filmId = f.film_id
		WHERE storeId = #{storeId}
		LIMIT #{beginRow}, #{rowPerPage}
	</select>
	
	
	<select id="select_UnReturned_InventoryTotalRowByStore" parameterType="Integer" resultType="Integer">
		SELECT COUNT(*)
		FROM film f
				INNER join
					(SELECT 
						filmId
						, storeId
						, GROUP_concat(inventoryId) inventoryId
						, COUNT(inventoryId) inventoryCount
					FROM 
						(SELECT 
							i.inventory_id inventoryId
							, i.store_id storeId
							, i.film_id filmId
						FROM inventory i
						INNER JOIN
							(SELECT 
								rental_id
								,inventory_id
								, rental_date
							FROM 
								(SELECT 
										rental_id
										, inventory_id
										, rental_date
										, return_date
								FROM rental V1
								JOIN 
									(SELECT 
										inventory_id inventoryId 
										, MAX(rental_date) maxRentalData
									 FROM rental 
									 GROUP BY inventory_id) V2
								 ON V1.inventory_id = V2.inventoryId AND V1.rental_date = V2.maxRentalData) V3
							 WHERE return_date IS NULL) V4
						 ON i.inventory_id = V4.inventory_id) V5
					GROUP BY storeId, filmId) V6
				ON V6.filmId = f.film_id
		WHERE storeId = #{storeId}
	</select>
	
	<select id="select_Returned_InventoryTotalRowByStore" parameterType="Integer" resultType="Integer">
		SELECT COUNT(*)
		FROM film f
				INNER join
					(SELECT 
						filmId
						, storeId
						, GROUP_concat(inventoryId) inventoryId
						, COUNT(inventoryId) inventoryCount
					FROM 
						(SELECT 
							i.inventory_id inventoryId
							, i.store_id storeId
							, i.film_id filmId
						FROM inventory i
						INNER JOIN
							(SELECT 
								rental_id
								,inventory_id
								, rental_date
							FROM 
								(SELECT 
										rental_id
										, inventory_id
										, rental_date
										, return_date
								FROM rental V1
								JOIN 
									(SELECT 
										inventory_id inventoryId 
										, MAX(rental_date) maxRentalData
									 FROM rental 
									 GROUP BY inventory_id) V2
								 ON V1.inventory_id = V2.inventoryId AND V1.rental_date = V2.maxRentalData) V3
							 WHERE return_date IS NOT NULL) V4
						 ON i.inventory_id = V4.inventory_id) V5
					GROUP BY storeId, filmId) V6
				ON V6.filmId = f.film_id
		WHERE storeId = #{storeId}
	</select>
</mapper>


